# API Configuration
# For local development, use your computer's local IP address (not localhost)
# Run 'ipconfig getifaddr en0' (Mac) or 'ipconfig' (Windows) to find your local IP
# API_BASE_URL=http://192.168.1.45:8000/v1

# Production API
API_BASE_URL=https://api.atlaskeswa.id/v1
